# PlanTogether Symfony Setup

This repository contains the base for a Symfony application using Symfony UX and Tailwind CSS.

## Installation

```bash
composer install
npm install
```

Run the Symfony server or any web server pointing to the `public/` directory.
